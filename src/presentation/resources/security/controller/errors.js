export const INVALID_USERNAME = () => ({ msg: 'Invalid username, expected a string' });
export const INVALID_PASSWORD = () => ({ msg: 'Invalid password, expected a string' });
export const UNEXPECTED = () => ({ msg: 'An unexpected error has happened' });
